spring: 
  profiles: 
    active: 
    - dev

---
spring:
  profiles: dev   # 开发环境
  application:
    name: microservicecloud-config-mycp-dev

---
spring:
  profiles: test # 测试环境
  application:
    name: microservicecloud-config-mycp-test

# 注意： 保存为UTF-8格式
